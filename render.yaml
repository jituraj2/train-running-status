services:
  - type: web
    name: train-status
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: API_KEY
        value: your_free_api_key_here
    autoDeploy: true
